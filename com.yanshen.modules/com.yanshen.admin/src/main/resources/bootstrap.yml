server:
  port: 3003
spring:
  application:
    name: com.yanshen.admin
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  profiles:
    # 环境配置 dev uat prod
    active: @spring.active@
