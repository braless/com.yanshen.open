server:
  port: 3001
spring:
  application:
    name: com.yanshen.auth
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  profiles:
    # 环境配置 dev uat prod
    active: @spring.active@

