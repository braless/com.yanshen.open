server:
  port: 3002
spring:
  application:
    name: com.yanshen.hotel
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  profiles:
    # 环境配置 dev uat prod
    active: @spring.active@
