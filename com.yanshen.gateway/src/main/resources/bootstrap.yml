server:
  port: 3000
spring:
  application:
    name: com.yanshen.gateway
  profiles:
    # 环境配置 dev uat prod
    active: @spring.active@
  # redis防重提交
redis:
  repeat:
    lock: NO_REPEAT_LOCK_PREFIX
